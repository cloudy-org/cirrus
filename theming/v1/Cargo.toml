[package]
name = "cirrus_theming"
version = "0.1.0"
edition = "2021"

[dependencies]
cirrus_path = { path = "../../path/v1" }
cirrus_error = { path = "../../error/v1" }

log = "0.4"
toml = "1.1"
serde = { version = "1.0", features = ["derive"] }