[package]
name = "cirrus_soft_binds_v1"
version = "0.1.0"
edition = "2024"

[features]
egui = ["dep:egui"]

[dependencies]
log = "0.4"
egui = { version = "0.33", default-features = false, optional = true }